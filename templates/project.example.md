# Agent Instructions — <project name>

Rules for this project only. The shared rules — communication, research, safety,
model escalation — come from `~/.dsh/AGENTS.md` and are already loaded; do not
repeat them here.

Keep this file short. Everything in it is read at the start of every session, so
write down what an agent would otherwise get wrong, not what it would get right
anyway.

## Stack

- Framework:
- Package manager:
- TypeScript or JavaScript:

## Conventions

- File structure:
- Naming:
- Libraries to use, and to avoid:

## Commands

- Test:            <!-- and whether to run it before reporting done -->
- Build:
- Dev server:

## Git

- Branch naming:
- Commit format:

## Anything else an agent would get wrong

<!--
The valuable part. Examples:

- `src/generated/` is machine-written — never edit it by hand.
- The staging API is flaky; a 502 there is not a bug in our code.
- We use tabs in `.tsv` fixtures on purpose. Do not let the formatter touch them.

Leave a section empty rather than filling it with guesses.
-->
